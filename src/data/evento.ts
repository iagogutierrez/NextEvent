/*export class evento<T> {
    id: T|undefined;    
    nombre_evento: string;
    fecha_inicio: Date;
    fecha_final: Date;
    descripcion: string;
    ubicacion: {
        nombre_ubicacion: string; 
        enlace: string;
    };
    tipo: string;
    organizador: string;
    pagina_web: string;
    precio: string;
    contacto: {
        email: string; 
        telefono: string; 
        redes_sociales: {
            facebook: string; 
            twitter: string
        } 
    };
    [];

    constructor(options: {
        id?: T;    
        nombre_evento?: string;
        fecha_inicio?: Date;
        fecha_final?: Date;
        descripcion?: string;
        ubicacion?: {
            nombre_ubicacion: string; 
            enlace: string;
        };
        tipo?: string;
        organizador?: string;
        pagina_web?: string;
        precio?: string;
        contacto?: {
            email: string; 
            telefono: string; 
            redes_sociales?: {
                facebook: string; 
                twitter: string
            } 
        };
    }= {}) {
    this.id = options.id;
    this.nombre_evento = options.nombre_evento || '';
    this.fecha_inicio = options.fecha_inicio || '';
    this.fecha_final = options.fecha_final || '';
    this.descripcion = options.descripcion || '';
    this.ubicacion = options.ubicacion || '';
    this.tipo = options.tipo || '';
    this.organizador = options.organizador || '';
    this.pagina_web = options.pagina_web || '';
    this.precio = options.precio || '';
    this.contacto = options.contacto || [
        
    ];

}*/